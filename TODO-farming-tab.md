# TODO — 농사 탭 (피드백 2026-08-29 "추천 비료/작물 조합")

> **상태: 1~3단계 구현 완료** (2026-09-17, #120). 남은 것은 6장(4단계 밭 배치도·열린 질문)뿐이다.
> 조사 결과 전체는 `docs/farming-research.md` (#117, #118). 운영 규칙은 CLAUDE.md "Farming Pipeline Rules", 화면 구조는 `docs/ui.md` "농사 탭".
>
> **결정된 것 (2026-09-17 사용자 확인)**: ① 새 탭 "농사" + `/farming` 정적 페이지 ② 스트레스 이름은 나무위키식 + 인게임 대사 보조 ("분위기 파괴자"는 규칙 설명에서 잡초·썩은 작물·텃밭 쓰레기로 풀어 씀) ③ 표·숫자 위주, "게임 수치"/"계산값" 구분 표기.
> 구현 중 정한 것: 계절은 텍스트 칩(봄·가을 아이콘이 게임에 없음), 양파 이미지는 게임 파일명 `quagmire_onion.png` 사용, 영양 블록 아이콘은 게임 아틀라스에서 추출.
> 아래 0~5장은 착수 전에 쓴 계획이다 — 기록으로 남긴다.

## 0. 시작 절차 (다음 세션이 할 일 순서대로)

1. **읽기**: 이 파일 → `docs/farming-research.md`의 0장·2.0절·7장 → `docs/ui.md`(화면 구조도, 공유 컴포넌트) → `docs/mistakes.md`의 "위키 서술을 '소스 기준' 문서에 확인 없이 옮겨 적음", "station 정보를 인게임 recipes.lua 검증 없이 작성", "한글명을 ko.po 확인 없이 임의 작성".
2. **재현 확인**: `bash scripts/sync-game-data.sh`(빌드가 같으면 즉시 종료) 후 `python3 scripts/farm-combos-prototype.py`. 기본 조합 수가 **봄 12 · 여름 4 · 가을 10 · 겨울 2**로 나오는지 본다. 다르면 게임 패치로 규칙이 바뀐 것이니 조사 문서부터 갱신한다.
3. **사용자 결정 3가지를 한 번에 확인** (1장). 답을 받기 전에는 UI 작업을 시작하지 않는다. 파이프라인(2.1)과 계산 모듈(2.2)은 결정과 무관하니 먼저 진행해도 된다.
4. `/task 농사 탭 1~3단계 구현 (파이프라인 + 참고표 + 계절별 조합 추천)` — base는 main. 다른 세션의 #110(시트 광고 제거, `DetailPanel.tsx`·`AdSlot.tsx` 수정)이 아직 main에 안 들어갔을 수 있으니 그 파일을 건드릴 땐 충돌에 주의.
5. 착수하면 피드백 `1788012304165-qq9emx`(농사 탭 요청)에 **hold** 답변을 단다. 문구는 사용자 확인 필수, 절차는 CLAUDE.md "Feedback Replies". 릴리즈 후 done 답변으로 교체하고 번역 백필.

## 1. 사용자 결정 대기 (2026-09-17 검토 때 제시, 아직 답 없음)

| # | 결정 | 추천안 | 대안 |
|---|---|---|---|
| 1 | **위치** | 새 탭 "농사"(10번째) + 검색용 정적 페이지. 피드백이 "농사 탭"을 직접 요청했고, 디시 조합 글 조회수 13만으로 검색 수요가 확인됨. 모바일 탭 바는 가로 스크롤이라 기술적 문제 없음 | 요리 탭이나 제작 탭 안의 한 구역 |
| 2 | **스트레스 7항목 이름** | 게임에 공식 라벨이 없다(캐릭터 대사만 있음). 나무위키식 이름(영양분·수분·분위기 파괴자·제철·가족·과밀·행복)을 쓰고 인게임 대사를 보조로 곁들임 | 자체 명명 |
| 3 | **설명 문구 수준** | 표·숫자 위주, 문장은 최소. "게임 수치"와 "우리가 계산한 값"을 화면에서 구분 표기 | 설명을 풍부하게 |

구현 중에 추가로 물어볼 것:
- **계절 아이콘**: `public/images/category-icons/`에는 winter·summer·rain만 있고 봄·가을 아이콘이 없다. 텍스트 칩으로 갈지, 인게임 계절 아이콘을 추출할지.
- **양파 작물 이미지**: `public/images/game-items/onion.png`만 없다(씨앗·거대 작물은 있음). 추출 또는 다운로드 필요.

## 2. 범위 — 1~3단계 (4단계 밭 배치도는 보류, 6장)

### 2.1 파이프라인 → `src/data/farming.ts` (자동 생성, 수동 편집 금지)
- 새 스크립트 `scripts/extract-farming.py`. `scripts/extract-raw-foods.py`를 본보기로: 입력 `/tmp/dst-extract/scripts`(스냅샷 심볼릭 링크), 출력 첫 줄 `// AUTO-GENERATED by scripts/extract-farming.py — DO NOT EDIT.`
- `scripts/sync-game-data.sh`의 converter 호출부(109~130행 부근)에 한 줄 추가.
- 읽을 소스와 뽑을 값:
  | 소스 | 값 |
  |---|---|
  | `prefabs/farm_plant_defs.lua` | 작물 14종: `nutrient_consumption`(S/M/L), `good_seasons`, `moisture.drink_rate`, `grow_time`(MakeGrowTimes 인자), product/seed id |
  | `prefabs/weed_defs.lua` | 잡초 4종: 양분 소비, drink_rate, seed_weight, spread |
  | `prefabs/veggies.lua` | `seed_weight`(COMMON/UNCOMMON/RARE 별칭 → `SEED_CHANCE_*`), perish, `OVERSIZED_PERISHTIME_MULT` |
  | `prefabs/fertilizer_nutrient_defs.lua` | 비료 id → `*_NUTRIENTS` 상수, `uses` |
  | `tuning.lua` | `FARM_PLANT_CONSUME_NUTRIENT_LOW/MED/HIGH`, `FARM_PLANT_DRINK_LOW/MED/HIGH`, `FARM_PLANT_DROUGHT_TOLERANCE`, `FARM_PLANT_KILLJOY_RADIUS`, `FARM_PLANT_SAME_FAMILY_MIN/RADIUS`, `FARM_PANT_OVERCROWDING_MAX_PLANTS`(소스 오타 그대로), `STARTING_NUTRIENTS_MIN/MAX`, `FARM_PLANT_RANDOMSEED_WEED_CHANCE`, `SEED_WEIGHT_SEASON_MOD`, `SEED_CHANCE_*`, `FARM_PLANT_LONG_LIFE_MULT`, `WATERINGCAN_WATER_AMOUNT`, `PREMIUMWATERINGCAN_WATER_AMOUNT`, `WATERINGCAN_USES`, `PREMIUMWATERINGCAN_USES`, `WATERBALLOON_ADD_WETNESS`, `ICE_MELT_GROUND_MOISTURE_AMOUNT`, `SOIL_RAIN_MOD`, `SOIL_MIN/MAX_TEMP_DRY_RATE`, 돌보기 범위(`HORN_RANGE`, `GNARWAIL_HORN_FARM_PLANT_INTERACT_RANGE`, `TRIDENT_FARM_PLANT_INTERACT_RANGE`, `PANFLUTE_SLEEPRANGE`, `ONEMANBAND_RANGE`, `PHONOGRAPH_TEND_RANGE`, `WORMWOOD_BLOOM_FARM_PLANT_INTERACT_RANGE`, `SINGINGSHELL_FARM_PLANT_INTERACT_RANGE`), `PERISH_GROUND_MULT` |
  | `components/farmplantstress.lua` | 스트레스 등급 경계(1 / 6 / 11) — 코드 상수가 아니라 함수 본문이라 정규식으로 뽑고 **assert로 고정** |
- **반드시 넣을 assert** (규칙이 바뀌면 빌드가 알려주게):
  - 작물의 소비 총량이 되돌릴 양분 개수로 나누어떨어진다 (아니면 소스의 "잉여 랜덤 분배" 경로가 발동해 계산식이 달라진다)
  - `TUNING.FORMULA_NUTRIENTS_INDEX == 1` (양분 배열 순서 = [성장 촉진제, 퇴비, 거름])
  - 작물 14종 모두 제철·물·양분이 있다 / `fertilizer_nutrient_defs.lua`의 모든 항목이 상수로 풀린다
- 이름: 작물은 `ko.ts` 등 로캘에 이미 있다(`cookpot-ingredients.ts`에도 `nameKo`). **씨앗 이름은 로캘에 없다** — ko.po에서 가져온다. 키는 확인해 두었다: `STRINGS.NAMES.<CROP>_SEEDS` = 도감 등록 전 모양 이름(당근 = "길쭉 씨앗"), `STRINGS.NAMES.KNOWN_<CROP>_SEEDS` = 작물 씨앗 이름("당근 씨앗"), `<CROP>_OVERSIZED` = 거대 작물("거대한 당근"), `FARM_PLANT_<CROP>` = 밭의 식물("당근 뿌리"). 나무위키의 씨앗 모양 이름("네모난 씨앗" 등)은 한글모드와 다르니 쓰지 말 것. 13개 로캘 po는 `~/dst-game-snapshot/scripts/languages/`에 있고 **CRLF**다(#111에서 정규식이 안 맞았던 원인). 한국어는 공식 korean.po가 아니라 **커뮤니티 한글모드 ko.po**.

### 2.2 조합 계산 모듈 → `src/lib/farming-combos.ts`
- `scripts/farm-combos-prototype.py`의 로직을 TS로 옮긴다: 양분 프로필 묶기 → 계절별 균형 조합(`balanced`) → 합성 조합 제외(`is_primitive`) → 타일당 포기 수(과밀 직전까지 채운 배수) → 가족 4포기를 한 타일로 채우는지 → 최악 순서 양분 수요 vs 새 밭 최소값.
- **조합은 데이터로 저장하지 않는다.** `farming.ts`에서 계산한다(빌드 시 또는 런타임). 패치를 자동으로 따라가고 출처 표기 부담이 없다.
- 가족 판정은 **작물 단위**다. 감자 2 + 가지 2는 양분상 감자 4와 같지만 가족은 못 채운다. 프로필로 묶는 건 "대체 가능" 표시에만 쓴다.
- 옮긴 뒤 프로토타입과 결과가 같은지 검증(4장)하고, 프로토타입 스크립트는 삭제한다 (CLAUDE.md Key Paths에서도 제거).

### 2.3 참고표 UI — 인게임 식물 도감을 그대로 옮긴다
- 아이템 스펙 패널이 스크랩북을 미러하듯, 작물 상세는 **인게임 식물 도감(Plant Registry)** 구성을 따른다. 라벨은 ko.po에 있다: `STRINGS.UI.PLANTREGISTRY.FARMPLANTS.SEASONS`(계절) / `.WATER`(물 소비량) / `.PRODUCT`(생산물), `.NUTRIENTS.CONSUME`(소비) / `.RESTORE`(배출) / `.NUTRIENT_1..3`(성장 촉진제·퇴비·거름), `.PLANT_GROWTH_STAGES.*`(씨앗·싹·작음·중간·성체·거대함·부패함·추대), `.DESCRIPTIONS.<CROP>`(작물별 한 줄 설명), `.EFFECTS.<WEED>`, `.TAB_TITLE_PLANTS` / `.TAB_TITLE_FERTILIZERS`.
- 화면: 작물 카드 그리드 → `DetailPanel`. 상세에는 제철, 양분 소비·배출, 물 소비량(저·중·고와 수치), 씨앗·생산물·거대 작물(`ItemSlot`), 일반 씨앗에서 나올 계절별 확률, **맞는 비료**(소비 양분에서 파생, `ItemSlot`), 이 작물이 들어간 조합 링크.
- 표 4개: 비료(양분값·사용 횟수), 돌보기 도구(타일 반경), 스트레스 7항목(규칙·수치), 스트레스 합계 → 수확물.
- 기존 컴포넌트만 쓴다: `ItemSlot`(아이템), `TagChip`(메타), `CategoryCard`, `DetailPanel`, `TabScrollArea` + `Footer`. 새 UI 패턴을 만들지 말 것 (CLAUDE.md UI Design Rules).

### 2.4 계절별 조합 추천 UI
- 계절 선택(4개) → 조합 줄 목록. 형식은 디시 계절별 표를 따른다: **칸 = 양분 역할, 칸 안에 대체 작물을 나란히, 숫자 = 타일당 포기**.
- 기본 조합 28개를 먼저 보여주고, 정렬은 ① 타일 하나로 끝나는 것 ② 작물 종류가 적은 것 ③ 일반 씨앗에서 흔한 작물 순. 프로필 4칸짜리·합성 조합은 접어 둔다.
- 줄마다 배지 둘: "타일 1개로 충분" / "같은 구성의 타일을 붙일 것", "새 밭에서 안전" / "비료 1회 권장: <비료>" (수요 > `STARTING_NUTRIENTS_MIN`일 때. 어떤 양분이 모자라는지에서 비료가 정해진다).
- 작물로 거르기("이 작물이 들어간 조합"). 작물 상세에서 넘어올 수 있게 URL 상태로.
- 조합 화면 어딘가에 **"일반 씨앗에서 자란 작물은 거대 작물이 될 수 없다"**를 반드시 표시한다. 공략 어디에도 없는 사실이고 조합을 따라 해도 안 되는 가장 흔한 이유가 된다.

### 2.5 통합 지점 (결정 1이 "새 탭"일 때)
- `src/components/AppShell.tsx`: `TabId` 유니온, `allTabs` 배열(이미지 후보: `/images/game-items/farm_plow_item.png` 또는 `farm_hoe.png`), **`isTabMounted("farming") && (...)` 블록을 함께** 추가(#91 탭 마운트 정책 — `docs/ui.md` AppShell 절).
- `src/lib/i18n.ts`: `tab_farming`(농사 / Farming)과 화면 문구 키. `t()`는 ko 외 전부 en으로 떨어진다.
- URL 상태: `?tab=farming&season=<s>&crop=<id>`. `useUrlStateSync` + `useTabSync`(#105 — 탭을 떠났다 돌아와도 시트가 남지 않게) 패턴을 따른다.
- 광고 자리: `AdSlot` 배치 규칙은 `docs/ui.md` AdSlot 절. 새 탭에 자리를 넣을지는 광고 수익 방침(메모리)에 따라 넣는 쪽이 기본.
- 끝나면 `docs/ui.md`(화면 구조도·공유 컴포넌트), `docs/terminology.md`(새 용어), `CLAUDE.md`(Key Paths + "Farming Pipeline Rules" 절), 릴리즈 노트(minor).

### 2.6 검색용 정적 페이지
- `/farming` + `/ko/farming`. 탭급 SSG의 본보기는 `src/app/cookpot/page.tsx`와 `src/app/ko/` 아래. `src/components/seo/labels.ts`, `src/app/sitemap.ts`, IndexNow는 배포 스크립트가 처리.
- 계절별 조합 표를 정적 HTML로 담는다(검색어: "굶지마 농사 조합", "DST giant crops combinations").

## 3. 반드시 지킬 것 (조사에서 나온 함정)
1. **"소스 기준"은 직접 읽은 코드만.** 상수 하나가 아니라 그 값이 쓰이는 함수까지 본다. 위키 서술은 미검증으로 표시. `docs/farming-research.md` 2.0절의 A/B/C 등급을 따르고, **C등급(미검증 4항목)은 화면에 사실로 쓰지 않는다.**
2. 위키가 틀린 값들을 다시 옮겨 적지 말 것: 물풍선은 **+20**, 얼음은 **+25**(위키는 반대), 축음기 돌보기 범위는 **2타일**(나무위키 4타일 ✗), 작물은 **성장 중일 때만** 물을 마신다(밤엔 0).
3. 이름은 전부 ko.po(한글모드). 토마토란, 성장 촉진제 원액/초 성장 촉진제, 거름통, 퇴비쌈, 나무 잼, 왱비료, 영양 블록, 텃밭 쟁기계, 괭이, 물뿌리개/물부리개, 농작물 등급 측정기, 텃밭 쓰레기, 건망초·뒤엎시라·불쐐기·가시올무메꽃. 자체 번역 금지.
4. 공략 원문·이미지를 레포에 넣지 않는다. 조합은 우리가 계산한 결과라 출처 표기가 필요 없지만, 표 형식 아이디어는 디시 글에서 왔다.
5. 실전 조언("단계마다 물 한 번" 등)은 우리 **계산값**이다. 게임 수치와 구분해 표기한다.
6. 메모리 규칙: 인게임 원문이 없는 설명을 길게 지어내지 않는다(`feedback_specialinfo_no_self_authoring`). 결정 3의 추천안이 이 원칙을 따른다.

## 4. 검증 계획
- 계산 모듈: 메모리 `verify-ts-logic-headless`의 Node 훅으로 `farming-combos.ts`를 실행해 **12 / 4 / 10 / 2**와 조합별 타일당 포기·수요 값이 프로토타입 출력(`docs/farming-research.md` 3.3절)과 같은지 비교.
- 일반 씨앗 확률표: 3.2절과 같은지 (위키는 올림 표기라 0.1 크다 — 오류 아님).
- `npx tsc --noEmit`, 변경 파일 eslint, `npm run build`. 워크트리에서는 node_modules 심볼릭 링크로 `tsc`는 되지만 **Turbopack 빌드는 거부**한다 → 빌드 전에 링크를 지우고 `npm ci`(5초 남짓).
- beta에서 `playwright-core` 헤드리스로 실측: 계절 전환, 작물 상세, 조합 거르기, 모바일 390px 가로 넘침, 영어, 다크모드. 크롬 확장은 이 환경에서 스크린샷·본문 추출이 안 됐다(#117).
- 릴리즈 뒤 `curl -sI https://www.dstcraft.com/ | grep -i x-vercel-id`로 Vercel 페일오버 잔존 확인 (release 스킬 7단계).

## 5. 완료 조건
- [x] `src/data/farming.ts`가 파이프라인으로 생성되고 `sync-game-data.sh`에 연결됨. assert 3종 포함
- [x] 조합 계산이 프로토타입과 일치, 프로토타입 스크립트 삭제
- [x] 참고표(작물·비료·도구·스트레스·수확물)와 계절별 조합 추천이 ko/en으로 동작
- [x] "일반 씨앗 → 거대 작물 불가" 안내 표시
- [x] `/farming`, `/ko/farming` 정적 페이지 + sitemap
- [ ] 문서 갱신: `docs/ui.md`, `docs/terminology.md`, `CLAUDE.md`, 릴리즈 노트, `todo.md`
- [ ] 피드백 `1788012304165-qq9emx` 답변(done) + 번역 백필

## 6. 보류·열린 질문
- **4단계 밭 배치도**: 3×3 격자에서 같은 작물 4포기를 일렬로 두면 양 끝 거리가 정확히 1타일(4.0)이다. 나무위키는 가족이 채워진다고 하고 Klei 가이드는 열 배치 모서리가 실패한다고 한다. 괭이질 좌표에 달린 문제라 **사용자의 인게임 확인**이 먼저다. 확인되면 좌표 기반 가족 검사로 "이 포기는 가족 미충족"을 표시할 수 있다.
- 양분 소비 순서 함정의 실제 빈도, 물·돌보기의 체감 주기 — 사용자 플레이 경험 필요.
- 미조사: 웜우드 직파·개화, WX-78 회로, 초파리 대왕 운영, 퇴비통 효율, 일반 씨앗에서 작물 씨앗을 불리는 초반 전략. 1~3단계에는 필요 없다.

## 7. 이력
- 2026-09-17 #117 조사(공략 10개 대조) · #118 정정(코드 경로 검증 수준) · #119 이 문서 · #120 1~3단계 구현
